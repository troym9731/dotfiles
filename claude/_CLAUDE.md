## Workflow
- DO NOT suggest code changes that include trailing whitespace! Always remove trailing whitespace.

## Planning
- All planning/brainstorm documents reside in `~/instinct/claude_plans`
- Downloadable artifacts should be placed in `~/instinct/claude_downloads`

## Version Control

I generally use `jj` (jujutsu) in git colocated repos. Use operations related to GitHub (`gh`),
but in general, use `jj` to explore.
