- defaults:
    link:
      relink: true
      create: true

- link:
    ~/.zshrc: zsh/home/zshrc
    ~/.gitconfig: git/home/gitconfig
    ~/.config/mise/config.toml: mise/home/config.toml
    ~/.aider.conf.yml: aider/conf.yml
