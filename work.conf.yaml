- defaults:
    link:
      relink: true
      create: true

- link:
    ~/.zshrc: zsh/work/zshrc
    ~/.gitconfig: git/work/gitconfig
    ~/.config/mise/config.toml: mise/work/config.toml
    ~/.aws/config: aws/work/config
